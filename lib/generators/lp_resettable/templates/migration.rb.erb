class <%= "#{migration_name.classify}s" %> < <%= migration_class_name %>
  def change
    add_column :<%= name.tableize %>, :reset_token, :string, default: nil
    add_column :<%= name.tableize %>, :reset_sent_at, :timestamp, default: nil

    add_index :<%= name.tableize %>, :reset_token
  end
end
